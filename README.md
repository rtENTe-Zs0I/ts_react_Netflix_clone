# Netflix Clone created by React+TypeScript

#Usage

* start with react-app
```bash
npx create-react-app ${projectname} --template typescript
```

* fix config
```json
# in tsconfig.json
# add this line
"noImplicitAny": false
```

* add module
```bash 
npm install node-sass
npm install movie-trailer
npm install react-youtube
```